"""Django application configuration for the oc_lettings_site application."""

from django.apps import AppConfig


class OCLettingsSiteConfig(AppConfig):
    """Configure the oc_lettings_site Django application."""

    name = "oc_lettings_site"
